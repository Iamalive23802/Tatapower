import { ComponentFixture, TestBed } from '@angular/core/testing';

import { LogsheetBoilerRow34Component } from './logsheet-boiler-row3-4.component';

describe('LogsheetBoilerRow34Component', () => {
  let component: LogsheetBoilerRow34Component;
  let fixture: ComponentFixture<LogsheetBoilerRow34Component>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [LogsheetBoilerRow34Component]
    });
    fixture = TestBed.createComponent(LogsheetBoilerRow34Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
